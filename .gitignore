# secrets & real config — NEVER commit
.env
.env.*
*.local
*.key
*-sa.json
**/auth.json
*.secret.*

# portunus runtime state (registry, audit log, approvals, clock)
.portunus/
registry.json
audit.log

# Hive session-runtime state (harness-generated, not source)
.pHive/agent-complete/
.pHive/interrupts/
.pHive/metrics/
.pHive/session-effort.txt

# python build / runtime
.venv/
venv/
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/
.coverage

# os
.DS_Store
.venv/
